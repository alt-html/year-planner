# S03: OpenAPI 3.x sync API spec

**Goal:** Write `api/openapi.yaml` defining the sync API contract. Covers auth token exchange, planner sync (push/pull), and account deletion.
**Demo:** `npx @redocly/cli lint api/openapi.yaml` passes with no errors.

## Tasks

- [x] **T01: Write OpenAPI spec** `est:20m`
