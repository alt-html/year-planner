# S01: Core framework swap — config, contexts, Boot.boot()

**Goal:** TBD
**Demo:** After this: app boots with v3 config/logger/cdi bundles; `ProfileAwareConfig` resolves env-specific API URL; services autowire logger correctly; boot E2E test passes.

## Tasks
