---
phase: 15-css-generalisation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .tests/smoke/css-generalisation.spec.js
  - site/css/design-tokens.css
  - site/css/rail.css
  - site/css/dots.css
  - site/css/main.css
autonomous: true
requirements: [CSS-01, CSS-02, CSS-03]

must_haves:
  truths:
    - "design-tokens.css contains all :root and [data-theme] custom property blocks"
    - "rail.css contains all .yp-rail*, .rail-flyout*, .rail-toggle rules"
    - "dots.css contains all .yp-dot, .yp-dot-c1..c8, .yp-dot-clear rules"
    - "main.css no longer contains the extracted blocks"
    - "Smoke test validates file existence and no duplication"
  artifacts:
    - path: "site/css/design-tokens.css"
      provides: "Theme custom property declarations"
      contains: ":root"
    - path: "site/css/rail.css"
      provides: "Rail navigation styles"
      contains: ".yp-rail"
    - path: "site/css/dots.css"
      provides: "Colour dot swatch styles"
      contains: ".yp-dot"
    - path: ".tests/smoke/css-generalisation.spec.js"
      provides: "Smoke test for CSS extraction"
  key_links:
    - from: "site/css/design-tokens.css"
      to: "site/css/main.css"
      via: "var() references in main.css consume tokens declared in design-tokens.css"
      pattern: "var\\(--"
---

<objective>
Extract design tokens, rail styles, and dot styles from main.css into three separate CSS files, and create a smoke test to validate the extraction.

Purpose: CSS-01, CSS-02, CSS-03 — decompose the 1380-line main.css into focused, maintainable files.
Output: Three new CSS files (design-tokens.css, rail.css, dots.css), a trimmed main.css, and a Playwright smoke test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-css-generalisation/15-RESEARCH.md
@site/css/main.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create smoke test for CSS generalisation</name>
  <files>.tests/smoke/css-generalisation.spec.js</files>
  <read_first>
    - .tests/smoke/compose.spec.js (reference pattern for smoke test structure)
    - .tests/smoke/dark-mode.spec.js (reference for CSS-related smoke tests)
    - .tests/playwright.config.js (test config)
  </read_first>
  <action>
Create `.tests/smoke/css-generalisation.spec.js` with Playwright tests that validate:

1. **File existence** — assert that `site/css/design-tokens.css`, `site/css/rail.css`, and `site/css/dots.css` exist (use Node.js `fs.existsSync` or `fs.readFileSync`).

2. **design-tokens.css content** — read the file and assert:
   - Contains `:root,` (the root block opener)
   - Contains `[data-theme="ink"]` 
   - Contains `[data-theme="crisp"]`
   - Does NOT contain any rule selectors like `.yp-rail` or `.yp-dot`

3. **rail.css content** — read the file and assert:
   - Contains `.yp-rail {`
   - Contains `.rail-flyout {`
   - Contains `.rail-toggle {`
   - Does NOT contain `:root`

4. **dots.css content** — read the file and assert:
   - Contains `.yp-dot {`
   - Contains `.yp-dot-clear`
   - Contains `.yp-dot-c8`
   - Contains `.marker-flyout button.yp-dot-c1`

5. **main.css no duplication** — read `site/css/main.css` and assert:
   - Does NOT contain `:root,` (tokens extracted)
   - Does NOT contain `.yp-rail {` (rail extracted)
   - Does NOT contain `.yp-dot {` (dots extracted)
   - Still contains `@media print` (the print rule stays)
   - Still contains `.yp-jumbotron` (non-extracted content remains)

6. **CSS-04 bare property check** (for Plan 02) — read all four CSS files and assert:
   - No file contains `var(--bg)` (without `yp-` prefix) — use regex `/var\(--(?!yp-|bs-)[a-z]/` to catch any bare custom property reference. This test is expected to FAIL until Plan 02 completes the rename. Mark it with `test.skip` and a comment `// Enable after CSS-04 rename in Plan 02`.

7. **CSS-05 head.html link check** (for Plan 02) — assert `site/index.html` contains `design-tokens.css`, `rail.css`, `dots.css` as link hrefs. Mark with `test.skip` and comment `// Enable after CSS-05 in Plan 02`.

Follow the existing test patterns from compose.spec.js — use `const { test, expect } = require('@playwright/test')` and `const fs = require('fs')` and `const path = require('path')`. Use `path.resolve(__dirname, '../../site/css/...')` to locate CSS files.
  </action>
  <verify>
    <automated>cd .tests && npx playwright test smoke/css-generalisation.spec.js --reporter=line 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `.tests/smoke/css-generalisation.spec.js` exists
    - File contains `require('@playwright/test')`
    - File contains `design-tokens.css`
    - File contains `rail.css`
    - File contains `dots.css`
    - File contains `test.skip` for CSS-04 and CSS-05 checks
    - Non-skipped tests pass (file existence tests will pass after Task 2 creates the files)
  </acceptance_criteria>
  <done>Smoke test file created; skipped tests documented for Plan 02 enablement</done>
</task>

<task type="auto">
  <name>Task 2: Extract design-tokens.css, rail.css, dots.css from main.css</name>
  <files>site/css/design-tokens.css, site/css/rail.css, site/css/dots.css, site/css/main.css</files>
  <read_first>
    - site/css/main.css (full file — the source of extraction)
    - .planning/phases/15-css-generalisation/15-RESEARCH.md (extraction boundaries and line ranges)
  </read_first>
  <action>
**Create `site/css/design-tokens.css`** — copy lines 1-108 of main.css (the `:root`/`[data-theme="ink"]` block at lines 11-59 and `[data-theme="crisp"]` block at lines 61-108). Add a file header comment:
```css
/* ============================================================
   YEAR PLANNER — DESIGN TOKENS
   Custom property declarations for both themes.
   Ink & Paper (ink) and Crisp & Clear (crisp).
   ============================================================ */
```
Do NOT include line 9 (`@media print { @page {size: auto !important} }`) — that stays in main.css.
Include the `:root, [data-theme="ink"] { ... }` block (lines 12-59) and `[data-theme="crisp"] { ... }` block (lines 62-108).

**Create `site/css/rail.css`** — copy these sections from main.css:
- Lines 301-386: `.yp-rail`, `.yp-rail.open`, `.yp-rail-logo`, `.yp-rail button`, `.yp-rail button:hover`, `.yp-rail button.active`, `.yp-rail-spacer`, `.yp-rail-bottom`, `.yp-rail-divider`, `.rail-theme-toggle`
- Lines 387-483: `.rail-flyout` through `.yp-rail button.flyout-active` (rail flyout, flyout items, settings flyout)
- Lines 608-649: `.rail-toggle` through `.rail-open .footer { margin-left: 48px; }`

Add a file header comment:
```css
/* ============================================================
   YEAR PLANNER — RAIL STYLES
   Left-side icon rail, flyout panels, and rail toggle button.
   ============================================================ */
```
Do NOT include lines 484-607 (marker flyout and emoji stamp mode sections) — those stay in main.css as they are content flyouts, not rail structure.

**Create `site/css/dots.css`** — copy these sections from main.css:
- Lines 1144-1175: `.yp-colour-dots`, `.yp-dot`, `.yp-dot:hover`, `.yp-dot-clear`, `.yp-dot-c1` through `.yp-dot-c8`
- Lines 530-538: `.marker-flyout button.yp-dot-c1` through `.marker-flyout button.yp-dot-c8` (the colour swatch overrides)

Add a file header comment:
```css
/* ============================================================
   YEAR PLANNER — DOT STYLES
   Colour dot swatches for entry modal and marker flyout.
   ============================================================ */
```
Include the comment on line 530 (`/* Colour swatches in marker flyout — must override rail button background:none */`).

**Trim `site/css/main.css`** — delete the extracted blocks:
- Delete lines 11-108 (the two theme blocks). Keep line 9 (`@media print`) and the file header comment (lines 1-7). The file should continue with `/* ---- RESPONSIVE CONTAINER ---- */` after the print rule.
- Delete lines 301-386 and 387-483 and 608-649 (the rail sections). Keep lines 484-607 (marker/emoji flyouts).
- Delete lines 530-538 (marker flyout dot overrides — now in dots.css).
- Delete lines 1144-1175 (entry modal dots — now in dots.css). Keep `.yp-entry-actions` which follows at line 1177.

After deletion, main.css should still contain: `@media print`, responsive container rules, base styles, navbar, jumbotron, grid/table, marker flyout (484-607 minus 530-538), emoji stamp, modals, footer, and all other non-extracted rules.

**Verification after extraction:**
- grep for `.yp-rail {` in main.css — should have 0 hits
- grep for `.yp-dot {` in main.css — should have 0 hits  
- grep for `:root,` in main.css — should have 0 hits
- grep for `.yp-jumbotron` in main.css — should still have hits
- grep for `.marker-flyout {` in main.css — should still have hits (marker flyout stays)
  </action>
  <verify>
    <automated>cd .tests && npx playwright test smoke/css-generalisation.spec.js --reporter=line 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `site/css/design-tokens.css` exists and contains `:root,`
    - `site/css/design-tokens.css` contains `[data-theme="crisp"]`
    - `site/css/design-tokens.css` contains `--btn-save-text`
    - `site/css/rail.css` exists and contains `.yp-rail {`
    - `site/css/rail.css` contains `.rail-flyout {`
    - `site/css/rail.css` contains `.rail-toggle {`
    - `site/css/rail.css` contains `margin-left: 48px`
    - `site/css/dots.css` exists and contains `.yp-dot {`
    - `site/css/dots.css` contains `.yp-dot-c8`
    - `site/css/dots.css` contains `.marker-flyout button.yp-dot-c1`
    - `site/css/main.css` does NOT contain `:root,`
    - `site/css/main.css` does NOT contain `.yp-rail {`
    - `site/css/main.css` does NOT contain `.yp-dot {`
    - `site/css/main.css` still contains `@media print`
    - `site/css/main.css` still contains `.marker-flyout {`
    - `site/css/main.css` still contains `.emoji-flyout`
    - All non-skipped smoke tests pass
  </acceptance_criteria>
  <done>Three CSS files extracted from main.css; main.css trimmed of all extracted blocks; no duplicate selectors across files; smoke tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed. This is a pure CSS file extraction — no user input, no API calls, no authentication. All files are static assets served to the browser.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | CSS files | accept | Static CSS files served from same origin; no elevated privilege. Standard web serving. |
</threat_model>

<verification>
1. Run `cd .tests && npx playwright test smoke/css-generalisation.spec.js --reporter=line` — all non-skipped tests green
2. Run `cd .tests && npx playwright test --reporter=line` — full suite green (no visual regressions from extraction alone, since files are not yet loaded via link tags)
3. Grep confirmation: `grep -c '.yp-rail {' site/css/main.css` returns 0; `grep -c '.yp-rail {' site/css/rail.css` returns 1
</verification>

<success_criteria>
- Three new CSS files exist with correct content extracted from main.css
- main.css contains no duplicated blocks (all extracted selectors removed)
- Smoke test validates file contents and non-duplication
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/15-css-generalisation/15-01-SUMMARY.md`
</output>
