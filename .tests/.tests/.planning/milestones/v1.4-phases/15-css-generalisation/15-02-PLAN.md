---
phase: 15-css-generalisation
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - site/css/design-tokens.css
  - site/css/rail.css
  - site/css/dots.css
  - site/css/main.css
  - site/css/yp-dark.css
  - .compose/fragments/head.html
  - site/index.html
  - .tests/smoke/css-generalisation.spec.js
autonomous: true
requirements: [CSS-04, CSS-05]

must_haves:
  truths:
    - "No bare custom property references remain — all var(--x) are var(--yp-x)"
    - "All theme declarations use --yp-* namespace"
    - "head.html loads design-tokens.css, rail.css, dots.css before main.css"
    - "index.html is regenerated from head.html via build.sh"
    - "App renders identically in both themes and dark mode"
  artifacts:
    - path: "site/css/design-tokens.css"
      provides: "Namespaced token declarations"
      contains: "--yp-bg"
    - path: "site/css/yp-dark.css"
      provides: "Dark mode with namespaced properties"
      contains: "--yp-bg"
    - path: ".compose/fragments/head.html"
      provides: "CSS load order with extracted files"
      contains: "design-tokens.css"
    - path: "site/index.html"
      provides: "Compiled output matching head.html"
      contains: "design-tokens.css"
  key_links:
    - from: ".compose/fragments/head.html"
      to: "site/index.html"
      via: "m4 build compilation"
      pattern: "design-tokens\\.css"
    - from: "site/css/design-tokens.css"
      to: "site/css/main.css"
      via: "var(--yp-*) references consuming --yp-* declarations"
      pattern: "var\\(--yp-"
---

<objective>
Rename all bare CSS custom properties to --yp-* namespace across all CSS files, update head.html to load extracted files, and rebuild index.html.

Purpose: CSS-04 (namespace custom properties) and CSS-05 (update CSS loading in head.html).
Output: All CSS files use --yp-* namespace; head.html loads new files; index.html rebuilt; all tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-css-generalisation/15-RESEARCH.md
@.planning/phases/15-css-generalisation/15-01-SUMMARY.md
@site/css/design-tokens.css
@site/css/rail.css
@site/css/dots.css
@site/css/main.css
@site/css/yp-dark.css
@.compose/fragments/head.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename bare custom properties to --yp-* namespace</name>
  <files>site/css/design-tokens.css, site/css/rail.css, site/css/dots.css, site/css/main.css, site/css/yp-dark.css</files>
  <read_first>
    - site/css/design-tokens.css (declarations to rename)
    - site/css/rail.css (var() references to rename)
    - site/css/dots.css (var() references to rename)
    - site/css/main.css (var() references to rename)
    - site/css/yp-dark.css (declarations and var() references to rename)
    - .planning/phases/15-css-generalisation/15-RESEARCH.md (rename inventory and collision avoidance)
  </read_first>
  <action>
Rename all 31 bare custom property names to `--yp-*` namespace. This affects both DECLARATIONS (e.g., `--bg: #FAF7F2`) and REFERENCES (e.g., `var(--bg)`).

**CRITICAL — Rename order to avoid substring collisions:**
Process LONGEST names first within each collision group. The collision groups are:

Group 1 (text-*):
1. `--text-light` → `--yp-text-light`
2. `--text-mid` → `--yp-text-mid`  
3. `--text` → `--yp-text` (AFTER the longer ones)

Group 2 (today-*):
1. `--today-radius` → `--yp-today-radius`
2. `--today-bg` → `--yp-today-bg`
3. `--today` → `--yp-today` (AFTER the longer ones)

Group 3 (rail-*):
1. `--rail-text-hover` → `--yp-rail-text-hover`
2. `--rail-text` → `--yp-rail-text` (AFTER hover)
3. `--rail-active-bg` → `--yp-rail-active-bg`
4. `--rail-bg` → `--yp-rail-bg` (AFTER active-bg)

Group 4 (btn-save-*):
1. `--btn-save-hover` → `--yp-btn-save-hover`
2. `--btn-save-text` → `--yp-btn-save-text`
3. `--btn-save-bg` → `--yp-btn-save-bg`

Group 5 (header-*):
1. `--header-border` → `--yp-header-border`

Group 6 (th-*):
1. `--th-transform` → `--yp-th-transform`
2. `--th-spacing` → `--yp-th-spacing`
3. `--th-weight` → `--yp-th-weight`
4. `--th-color` → `--yp-th-color`
5. `--th-size` → `--yp-th-size`
6. `--th-bg` → `--yp-th-bg`

Group 7 (no-collision singles — safe in any order):
- `--font-display` → `--yp-font-display`
- `--font-body` → `--yp-font-body`
- `--font-mono` → `--yp-font-mono`
- `--bg` → `--yp-bg` (do AFTER `--rail-bg`, `--today-bg`, `--th-bg`, `--btn-save-bg`)
- `--surface` → `--yp-surface`
- `--accent` → `--yp-accent`
- `--weekend` → `--yp-weekend`
- `--past` → `--yp-past`
- `--border` → `--yp-border` (do AFTER `--header-border`)
- `--grid-td-size` → `--yp-grid-td-size`
- `--c2` through `--c8` → `--yp-c2` through `--yp-c8`

**Files to process (all five):**
- `site/css/design-tokens.css` — all declarations + any `var()` self-references (e.g., `--font-mono: var(--font-body)`)
- `site/css/rail.css` — `var()` references to token properties
- `site/css/dots.css` — `var()` references to token properties
- `site/css/main.css` — `var()` references throughout
- `site/css/yp-dark.css` — declarations in `.yp-dark` and `.yp-dark[data-theme="crisp"]` blocks + any `var()` references

**Implementation approach:** Use sed or manual find-replace. For each property, replace ALL of these patterns:
- Declaration: `--name:` → `--yp-name:` (in property declarations)
- Reference: `var(--name)` → `var(--yp-name)`  
- Reference with fallback: `var(--name,` → `var(--yp-name,`

**After rename, verify NO double-prefixing occurred:**
- Search all 5 files for `--yp-yp-` — must return 0 hits
- Search all 5 files for `var(--bg)` (without yp) — must return 0 hits
- Search all 5 files for `var(--text)` (without yp) — must return 0 hits
- Search for `var(--yp-bg)` — must return hits (confirming rename worked)
  </action>
  <verify>
    <automated>grep -r 'var(--bg)' site/css/ 2>/dev/null | grep -v yp- | wc -l && grep -r '\-\-yp-yp-' site/css/ 2>/dev/null | wc -l && grep -c 'var(--yp-bg)' site/css/main.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'var(--bg)' site/css/` returns 0 lines (no bare --bg references)
    - `grep -rn '\-\-yp-yp-' site/css/` returns 0 lines (no double prefix)
    - `grep -c 'var(--yp-bg)' site/css/main.css` returns a positive number
    - `grep -c '\-\-yp-bg:' site/css/design-tokens.css` returns at least 2 (ink + crisp declarations)
    - `grep -c '\-\-yp-bg:' site/css/yp-dark.css` returns at least 2 (dark ink + dark crisp)
    - `grep -c 'var(--yp-text)' site/css/main.css` returns a positive number
    - `grep -c 'var(--yp-surface)' site/css/main.css` returns a positive number
    - `grep -c 'var(--yp-c2)' site/css/dots.css` returns a positive number
    - `grep -c 'var(--yp-rail-bg)' site/css/rail.css` returns a positive number
    - No bare custom property names from the 31-property inventory remain as declarations or var() references in any CSS file
  </acceptance_criteria>
  <done>All 31 bare custom properties renamed to --yp-* namespace across all 5 CSS files; no double-prefixing; no orphaned bare references</done>
</task>

<task type="auto">
  <name>Task 2: Update head.html, rebuild index.html, enable smoke tests</name>
  <files>.compose/fragments/head.html, site/index.html, .tests/smoke/css-generalisation.spec.js</files>
  <read_first>
    - .compose/fragments/head.html (current CSS loading — must add new link tags)
    - .tests/smoke/css-generalisation.spec.js (un-skip the CSS-04 and CSS-05 tests)
    - .planning/phases/15-css-generalisation/15-RESEARCH.md (target load order)
  </read_first>
  <action>
**Edit `.compose/fragments/head.html`** — replace the current app stylesheet block:
```html
<link rel="stylesheet" href="./css/main.css">
<link rel="stylesheet" href="./css/yp-dark.css">
<link rel="stylesheet" href="./css/typeaheadjs.css">
```
with:
```html
<link rel="stylesheet" href="./css/design-tokens.css">
<link rel="stylesheet" href="./css/rail.css">
<link rel="stylesheet" href="./css/dots.css">
<link rel="stylesheet" href="./css/main.css">
<link rel="stylesheet" href="./css/yp-dark.css">
<link rel="stylesheet" href="./css/typeaheadjs.css">
```

The three new links go BEFORE `main.css` so custom property declarations are available when `main.css` `var()` references are parsed. The order within the new files does not matter since they have no selector overlap.

**Run build.sh** to regenerate `site/index.html`:
```bash
bash .compose/build.sh
```

**Verify** that `site/index.html` contains all six `<link>` tags in the correct order.

**Enable skipped smoke tests** in `.tests/smoke/css-generalisation.spec.js`:
- Change `test.skip(` to `test(` for the CSS-04 bare property check
- Change `test.skip(` to `test(` for the CSS-05 head.html link check
- Remove or update the skip comments

**Run full test suite** to confirm no regressions.
  </action>
  <verify>
    <automated>bash .compose/build.sh && cd .tests && npx playwright test --reporter=line 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `.compose/fragments/head.html` contains `href="./css/design-tokens.css"`
    - `.compose/fragments/head.html` contains `href="./css/rail.css"`
    - `.compose/fragments/head.html` contains `href="./css/dots.css"`
    - `design-tokens.css` link appears BEFORE `main.css` link in head.html
    - `site/index.html` contains `design-tokens.css` (build output matches source)
    - `site/index.html` contains `rail.css`
    - `site/index.html` contains `dots.css`
    - `.tests/smoke/css-generalisation.spec.js` has no `test.skip` remaining
    - `cd .tests && npx playwright test --reporter=line` exits 0 (all tests pass)
    - `cd .tests && npx playwright test smoke/compose.spec.js --reporter=line` exits 0 (idempotent build check passes)
  </acceptance_criteria>
  <done>head.html updated with new CSS links; index.html rebuilt; all smoke tests enabled and passing; full test suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed. CSS file loading order and custom property namespacing are purely client-side rendering concerns. No user input processed, no server interaction.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02 | Tampering | CSS files | accept | Static CSS files served from same origin; custom property rename has no security implication |
</threat_model>

<verification>
1. Run `cd .tests && npx playwright test smoke/css-generalisation.spec.js --reporter=line` — all tests pass (including CSS-04 and CSS-05 checks)
2. Run `cd .tests && npx playwright test smoke/compose.spec.js --reporter=line` — idempotent build check passes
3. Run `cd .tests && npx playwright test --reporter=line` — full suite green
4. Static check: `grep -rn 'var(--bg)' site/css/` returns 0 hits (all bare properties renamed)
5. Static check: `grep -rn '\-\-yp-yp-' site/css/` returns 0 hits (no double-prefix)
</verification>

<success_criteria>
- All 31 bare custom properties renamed to --yp-* across all 5 CSS files
- head.html loads design-tokens.css, rail.css, dots.css before main.css
- index.html regenerated and matches build output
- All Playwright tests pass (smoke + e2e)
- No visual regressions
</success_criteria>

<output>
After completion, create `.planning/phases/15-css-generalisation/15-02-SUMMARY.md`
</output>
