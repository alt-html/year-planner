{
  "name": "year-planner-tests",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.58.0",
    "http-server": "^14.1.1"
  },
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:smoke": "playwright test smoke/ --reporter=line",
    "test:icon-signoff": "bash ../scripts/verify-icon-integration-signoff.sh"
  }
}
