---
phase: 12-auth-client-configuration-live-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .compose/index.html.m4
  - .compose/fragments/rail.html
  - .compose/fragments/scripts.html
  - .compose/fragments/modals/auth.html
  - site/js/vue/model/ui.js
  - site/js/vue/methods/auth.js
  - site/js/vue/methods/lifecycle.js
  - site/js/vue/methods/planner.js
  - site/js/vue/app.js
  - site/index.html
autonomous: true
requirements:
  - MOD-09
must_haves:
  truths:
    - "Rail buttons (calendar, share, marker, emoji, menu, sign-in/out, theme, dark) respond to clicks via Vue bindings inside #app"
    - "Auth modal opens and closes without jQuery — showSignin(), closeAuthModal(), signInWith() use Vue data flag"
    - "Marker mode and emoji mode work via Vue data properties, not bridge script closures"
    - "Calendar flyout, marker flyout, emoji flyout toggle via Vue v-show"
    - "jQuery, Popper.js, and Bootstrap JS CDN script tags are removed from scripts.html"
    - "Bridge script IIFE (~520 lines) is deleted from scripts.html"
    - "All 5 modal fragments in .compose/fragments/modals/ are referenced in modals.html (MOD-09)"
  artifacts:
    - path: ".compose/index.html.m4"
      provides: "rail.html included INSIDE #app div"
      contains: "rail.html"
    - path: ".compose/fragments/rail.html"
      provides: "Vue-bound rail buttons"
      contains: "v-on:click"
    - path: "site/js/vue/model/ui.js"
      provides: "Rail/flyout/modal Vue data properties"
      contains: "showAuthModal"
    - path: "site/js/vue/methods/auth.js"
      provides: "jQuery-free modal control"
      contains: "this.showAuthModal"
    - path: ".compose/fragments/scripts.html"
      provides: "No jQuery CDN, no bridge IIFE"
  key_links:
    - from: ".compose/fragments/rail.html"
      to: "site/js/vue/app.js"
      via: "Vue v-on:click bindings call methods defined in app.js"
      pattern: "v-on:click"
    - from: "site/js/vue/methods/auth.js"
      to: "site/js/vue/model/ui.js"
      via: "showAuthModal data flag"
      pattern: "this\\.showAuthModal"
---

<objective>
Migrate the vertical rail from outside #app into the Vue template, replace the ~520-line bridge script with Vue data/methods, remove jQuery/Popper/Bootstrap JS CDN dependencies, and replace all jQuery modal/show/hide calls with Vue data flags.

Purpose: The rail lives outside Vue's `#app` div, requiring a massive bridge script that uses private Vue internals (`_vnode.component.proxy`) and jQuery. Moving rail inside `#app` eliminates the bridge entirely, drops jQuery as a dependency, and makes all rail interactions first-class Vue bindings. Per D-05, UI must work before sync debugging. Per D-06, full Vue migration of the rail. Per D-07, drop jQuery/Bootstrap JS.

Output: Working rail with Vue bindings, no jQuery dependency, rebuilt site/index.html.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-auth-client-configuration-live-sync/12-CONTEXT.md
@.planning/phases/12-auth-client-configuration-live-sync/12-RESEARCH.md

@.compose/index.html.m4
@.compose/fragments/rail.html
@.compose/fragments/scripts.html
@.compose/fragments/modals/auth.html
@.compose/fragments/nav.html
@.compose/fragments/modals.html
@site/js/vue/app.js
@site/js/vue/model.js
@site/js/vue/model/ui.js
@site/js/vue/methods/auth.js
@site/js/vue/methods/lifecycle.js
@site/js/vue/methods/planner.js

<interfaces>
<!-- From site/js/vue/model/ui.js — current state to extend -->
export const uiState = {
    rename: false,
    error: '',
    warning: '',
    modalError: '',
    modalErrorTarget: null,
    modalWarning: '',
    modalSuccess: '',
    loaded: false,
    touch: '',
    _showSigninPester: false,
    railCollapsed: false,
}

<!-- From site/js/vue/app.js — method spread pattern -->
import { calendarMethods } from './methods/calendar.js';
import { entryMethods } from './methods/entries.js';
import { plannerMethods } from './methods/planner.js';
import { authMethods } from './methods/auth.js';
import { lifecycleMethods } from './methods/lifecycle.js';
// methods: { ...calendarMethods, ...entryMethods, ...plannerMethods, ...authMethods, ...lifecycleMethods }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Vue data properties and rail methods, restructure m4 compose</name>
  <read_first>
    - site/js/vue/model/ui.js
    - site/js/vue/app.js
    - site/js/vue/model.js
    - .compose/index.html.m4
    - .compose/fragments/scripts.html
    - .compose/fragments/rail.html
    - .compose/fragments/modals/auth.html
    - site/js/vue/methods/auth.js
    - site/js/vue/methods/lifecycle.js
    - site/js/vue/methods/planner.js
  </read_first>
  <files>
    site/js/vue/model/ui.js,
    site/js/vue/methods/rail.js,
    site/js/vue/app.js,
    .compose/index.html.m4
  </files>
  <action>
**1. Add rail/flyout/modal data properties to `site/js/vue/model/ui.js`:**

Add these properties to the `uiState` export (keep all existing properties):

```javascript
// Rail flyout state
railFlyout: null,        // 'calendar' | 'marker' | 'emoji' | null
// Marker mode
markerActive: false,
markerColour: 1,
// Emoji mode
emojiActive: false,
emojiSelected: '',
// Modal flags (replace jQuery .modal('show')/.modal('hide'))
showAuthModal: false,
showShareModal: false,
// Rename form visibility (replace jQuery $('#rename').show()/hide())
renameVisible: false,
// Nav menu dropdown
navMenuOpen: false,
```

**2. Create `site/js/vue/methods/rail.js`:**

Create a new methods file exporting `railMethods` with these methods:

- `toggleFlyout(name)`: Sets `this.railFlyout = this.railFlyout === name ? null : name`. If switching away from marker/emoji mode, deactivate that mode first.
- `toggleMarkerMode()`: If `this.markerActive`, call `deactivateMarkerMode()`. Else: if `this.emojiActive` call `deactivateEmojiMode()` first, then set `this.markerActive = true`, `document.body.classList.add('marker-mode')`, `this.railFlyout = 'marker'`.
- `deactivateMarkerMode()`: Set `this.markerActive = false`, `document.body.classList.remove('marker-mode')`, `document.body.classList.remove('marker-dragging')`, if `this.railFlyout === 'marker'` set `this.railFlyout = null`.
- `selectMarkerColour(colour)`: Set `this.markerColour = parseInt(colour)`.
- `toggleEmojiMode()`: Mirror of toggleMarkerMode but for emoji. If `this.emojiActive`, call `deactivateEmojiMode()`. Else: if `this.markerActive` call `deactivateMarkerMode()` first, then `this.emojiActive = true`, `document.body.classList.add('emoji-mode')`, `this.railFlyout = 'emoji'`.
- `deactivateEmojiMode()`: `this.emojiActive = false`, `document.body.classList.remove('emoji-mode')`, `document.body.classList.remove('emoji-dragging')`, if `this.railFlyout === 'emoji'` set `this.railFlyout = null`.
- `selectEmoji(emoji)`: Set `this.emojiSelected = emoji`.
- `selectEmojiTab(tab)`: This is purely a DOM class toggle for emoji tab panels — use `this.$el.querySelectorAll('.emoji-tab-btn').forEach(b => b.classList.remove('active'))` then find the clicked button and add 'active'. Same for panels. (Or use a Vue data property `emojiTab: 'faces'` and `v-bind:class` in template.)
- `toggleNavMenu()`: Toggle `this.navMenuOpen`. When closing, remove document click listener. When opening, add a document click listener (via `setTimeout(() => ..., 0)`) that closes menu if click is outside `.btn-group`. Store listener ref on `this._closeNavMenu`.
- `closeAuthModal()`: Set `this.showAuthModal = false`.
- `closeShareModal()`: Set `this.showShareModal = false`.
- `applyMarkerToCell(cellEl)`: Port the logic from scripts.html `applyMarkerToCell()` but instead of using `_vnode.component.proxy`, call `this.getEntry()`, `this.getEntryType()`, etc. directly (they are sibling Vue methods). Parse month index and day from DOM position exactly as the bridge does.
- `applyEmojiToCell(cellEl)`: Same pattern as applyMarkerToCell but for emoji — call `this.getEntry()`, `this.getEntryType()`, `this.getEntryColour()`, `this.getEntryNotes()`, then `this.updateEntry(...)` with `this.emojiSelected`.
- `initRailInteractions()`: Set up document-level event listeners for marker/emoji drag painting. This replaces the bridge's mousedown/mousemove/mouseup handlers. Use capture phase for mousedown/click to intercept before modal. Track `_markerDragging`, `_markerLastCell`, `_emojiDragging`, `_emojiLastCell` as instance properties (not reactive — prefix with `_`). Call this from `mounted()`.

Keep the rail toggle (open/close), dark mode toggle, style theme toggle, year input, and calendar flyout month link handlers as part of the bridge's non-jQuery vanilla JS — these do NOT need Vue methods because they operate on the rail element itself. However, since rail is now inside #app, these must become Vue methods too (they reference `rail` and `body` via getElementById). Add:

- `toggleRail()`: `this.$el.querySelector('#rail').classList.toggle('open'); document.body.classList.toggle('rail-open'); this.railFlyout = null;`
- `doDarkToggle()`: Port the dark mode URL-param toggle from bridge script. Persist rail state to sessionStorage if rail is open.
- `toggleStyleTheme()`: Toggle `data-theme` between 'ink' and 'crisp' on `document.body`, save to localStorage, update theme label.
- `navigateToYear()`: Read value from the year input, construct URL params, navigate.

**3. Register rail methods in `site/js/vue/app.js`:**

Add import: `import { railMethods } from './methods/rail.js';`
Add to methods spread: `...railMethods,`
In `mounted()`, after `this.refresh()`, add `this.initRailInteractions();` to set up document event listeners.

**4. Move rail include inside #app in `.compose/index.html.m4`:**

Change from:
```
<body>
m4_include([[[.compose/fragments/rail.html]]])m4_dnl
    <div id="app" ...>
```

To:
```
<body>
    <div id="app" v-bind:class="{'yp-dark':theme=='dark'}">
m4_include([[[.compose/fragments/rail.html]]])m4_dnl
```

The rail include moves INSIDE the `<div id="app">` opening tag. Keep it as the first child of #app (before spinner.html).
  </action>
  <verify>
    <automated>cd /Users/craig/src/github/alt-html/year-planner && grep -c "v-on:click\|v-if\|v-show\|v-bind:class" .compose/fragments/rail.html && grep -c "showAuthModal\|railFlyout\|markerActive\|emojiActive" site/js/vue/model/ui.js && grep -c "railMethods" site/js/vue/app.js && grep "rail.html" .compose/index.html.m4</automated>
  </verify>
  <acceptance_criteria>
    - site/js/vue/model/ui.js contains `showAuthModal: false`
    - site/js/vue/model/ui.js contains `railFlyout: null`
    - site/js/vue/model/ui.js contains `markerActive: false`
    - site/js/vue/model/ui.js contains `emojiActive: false`
    - site/js/vue/model/ui.js contains `showShareModal: false`
    - site/js/vue/model/ui.js contains `renameVisible: false`
    - site/js/vue/model/ui.js contains `navMenuOpen: false`
    - site/js/vue/methods/rail.js exists and contains `export const railMethods`
    - site/js/vue/methods/rail.js contains `toggleFlyout(`
    - site/js/vue/methods/rail.js contains `toggleMarkerMode(`
    - site/js/vue/methods/rail.js contains `toggleEmojiMode(`
    - site/js/vue/methods/rail.js contains `applyMarkerToCell(`
    - site/js/vue/methods/rail.js contains `applyEmojiToCell(`
    - site/js/vue/methods/rail.js contains `initRailInteractions(`
    - site/js/vue/methods/rail.js contains `toggleNavMenu(`
    - site/js/vue/methods/rail.js contains `closeAuthModal(`
    - site/js/vue/app.js contains `import { railMethods }`
    - site/js/vue/app.js contains `...railMethods`
    - site/js/vue/app.js mounted() contains `this.initRailInteractions()`
    - .compose/index.html.m4 has rail.html include AFTER `<div id="app"` line (rail inside #app)
    - .compose/index.html.m4 does NOT have rail.html include BEFORE `<div id="app"` line
  </acceptance_criteria>
  <done>Vue model has all rail/flyout/modal data properties. Rail methods file created with all interaction handlers. app.js registers railMethods. m4 compose has rail inside #app.</done>
</task>

<task type="auto">
  <name>Task 2: Convert rail.html to Vue bindings, replace jQuery in Vue methods, delete bridge, remove jQuery CDN, rebuild</name>
  <read_first>
    - .compose/fragments/rail.html
    - .compose/fragments/scripts.html
    - .compose/fragments/modals/auth.html
    - .compose/fragments/nav.html
    - site/js/vue/methods/auth.js
    - site/js/vue/methods/lifecycle.js
    - site/js/vue/methods/planner.js
    - .compose/fragments/modals.html
  </read_first>
  <files>
    .compose/fragments/rail.html,
    .compose/fragments/scripts.html,
    .compose/fragments/modals/auth.html,
    .compose/fragments/nav.html,
    site/js/vue/methods/auth.js,
    site/js/vue/methods/lifecycle.js,
    site/js/vue/methods/planner.js,
    site/index.html
  </files>
  <action>
**1. Convert `.compose/fragments/rail.html` to Vue bindings (per D-06):**

Remove the comment "outside #app -- plain JS, no Vue". Replace all `id=`-based button references with Vue bindings:

```html
<nav class="yp-rail" id="rail">
    <div class="yp-rail-logo">YP</div>
    <button class="active" title="Calendar" v-on:click="toggleFlyout('calendar')"><i class="ph ph-calendar-dots"></i></button>
    <button title="Share" v-on:click="sharePlanner()"><i class="ph ph-share-network"></i></button>
    <button title="Highlighter" v-on:click="toggleMarkerMode()" v-bind:class="{active: markerActive}"><i class="ph ph-highlighter-circle"></i></button>
    <button title="Emoji stamp" v-on:click="toggleEmojiMode()" v-bind:class="{active: emojiActive}"><i class="ph ph-smiley-sticker"></i></button>
    <div class="yp-rail-spacer"></div>
    <div class="yp-rail-bottom">
        <button title="Language"><i class="ph ph-globe-simple"></i></button>
        <button title="Menu" v-on:click="toggleNavMenu()"><i class="ph ph-gear-six"></i></button>
        <button v-if="!signedin" title="Sign in" v-on:click="showSignin()"><i class="ph ph-sign-in"></i></button>
        <button v-if="signedin" title="Sign out" v-on:click="signout()"><i class="ph ph-sign-out"></i></button>
        <div class="yp-rail-divider"></div>
        <button class="rail-theme-toggle" v-on:click="toggleStyleTheme()" title="Switch theme">
            <i class="ph ph-ruler" v-show="!styleCrisp"></i>
            <i class="ph ph-feather" v-show="styleCrisp"></i>
        </button>
        <button v-on:click="doDarkToggle()" title="Toggle dark mode"><i class="ph ph-sun-dim"></i></button>
    </div>

    <!-- Calendar flyout -->
    <div class="rail-flyout" v-show="railFlyout === 'calendar'">
        <!-- month links: keep same structure, add v-on:click to close rail -->
        ...same month/today links...
        <input type="text" maxlength="4" class="rail-flyout-input" placeholder="Year..." v-on:keydown.enter.prevent="navigateToYear($event)">
    </div>

    <!-- Marker flyout -->
    <div class="rail-flyout marker-flyout" v-show="railFlyout === 'marker'">
        <div class="marker-flyout-dots">
            <button class="marker-dot marker-dot-clear" v-on:click="selectMarkerColour(0)" v-bind:class="{selected: markerColour === 0}" title="Eraser"><i class="ph ph-eraser"></i></button>
            <!-- repeat for colours 1-8, with v-bind:class="{selected: markerColour === N}" -->
            <button class="marker-dot yp-dot-c1" v-on:click="selectMarkerColour(1)" v-bind:class="{selected: markerColour === 1}"></button>
            ...etc for 2-8...
        </div>
    </div>

    <!-- Emoji flyout -->
    <div class="rail-flyout emoji-flyout" v-show="railFlyout === 'emoji'">
        <!-- Keep same emoji content, but add Vue bindings to tab buttons and stamp buttons -->
        <div class="emoji-eraser-row">
            <button class="emoji-stamp-btn" v-bind:class="{selected: emojiSelected === ''}" v-on:click="selectEmoji('')" title="Eraser"><i class="ph ph-eraser"></i></button>
        </div>
        <div class="emoji-tab-bar">
            <!-- tab buttons get v-on:click="selectEmojiTab('faces')" etc -->
        </div>
        <!-- emoji stamp buttons get v-on:click="selectEmoji('...')" and v-bind:class="{selected: emojiSelected === '...'}" -->
    </div>
</nav>

<!-- Rail toggle button (stays outside rail nav but inside #app) -->
<button class="rail-toggle" v-on:click="toggleRail()" title="Menu">
    <i class="ph ph-caret-right"></i>
    <i class="ph ph-caret-left"></i>
</button>

<!-- Theme label -->
<div class="theme-label" id="themeLabel"></div>

<!-- Floating dark mode toggle -->
<div class="floating-controls">
    <button class="float-btn" v-on:click="doDarkToggle()" title="Toggle dark mode">
        <i class="ph ph-sun-dim"></i>
    </button>
</div>
```

Remove ALL `id=` attributes that were only used by the bridge script: `railCalendarBtn`, `railMarkerBtn`, `railEmojiBtn`, `railMenuBtn`, `railSigninBtn`, `railDarkToggle`, `themeToggleBtn`, `themeSwitchIconRuler`, `themeSwitchIconFeather`, `railCalendarFlyout`, `railMarkerFlyout`, `railEmojiFlyout`, `railYearInput`, `railToggle`, `darkToggle`. Keep `id="rail"` (used for CSS and outside-click detection).

**2. Replace jQuery calls in `site/js/vue/methods/auth.js`:**

```javascript
export const authMethods = {
    showSignin() {
        this.clearModalAlert();
        this.showAuthModal = true;  // was: $('#authModal').modal('show')
    },
    async signInWith(provider) {
        this.clearModalAlert();
        try {
            await this.authProvider.signIn(provider);
            this.showAuthModal = false;  // was: $('#authModal').modal('hide')
            this.signedin = true;
            this.userKey = this.plannerStore.getUserKey();
            this.syncScheduler.markDirty();
        } catch (err) {
            this.modalError = err.message || 'error.syncfailed';
        }
    },
    // signout() and clearModalAlert() unchanged
}
```

**3. Replace jQuery call in `site/js/vue/methods/lifecycle.js` line 37:**

Change `jQuery('#authModal').modal('show')` to `this.showAuthModal = true`:
```javascript
if (this._showSigninPester) {
    this._showSigninPester = false;
    this.$nextTick(() => { this.showAuthModal = true; });
}
```

**4. Replace jQuery calls in `site/js/vue/methods/planner.js`:**

- `showRenamePlanner()`: Replace `$('#rename').show()` and `$('#title').focus()` with:
  ```javascript
  showRenamePlanner() {
      this.syncScheduler.markDirty();
      this.rename = true;
      this.renameVisible = true;
      this.$nextTick(() => {
          const titleInput = this.$el.querySelector('#title');
          if (titleInput) titleInput.focus();
      });
  },
  ```
- `renamePlanner()`: Replace `$('#rename').hide()` with `this.renameVisible = false`:
  ```javascript
  renamePlanner() {
      this.renameVisible = false;
      // ... rest unchanged
  },
  ```
- `sharePlanner()`: Replace `$('#shareModal').modal('show')` with `this.showShareModal = true`:
  ```javascript
  sharePlanner() {
      this.showShareModal = true;
      this.shareUrl = window.location.origin + '?share=' + this.storage.getExportString();
      this.$nextTick(() => {
          const copyText = document.getElementById('copyUrl');
          if (copyText) { copyText.select(); copyText.setSelectionRange(0, 99999); }
      });
  },
  ```

**5. Update `.compose/fragments/modals/auth.html` — replace data-dismiss with Vue bindings:**

- Close button (header X): replace `data-dismiss="modal"` with `v-on:click="closeAuthModal()"`
- Apple button: replace `data-dismiss="modal"` with nothing (signInWith already closes)
- Microsoft button: same — remove `data-dismiss="modal"`
- Footer close button: replace `data-dismiss="modal"` with `v-on:click="closeAuthModal()"`
- Add Vue class bindings on modal div:
  ```html
  <div class="modal fade" id="authModal"
       v-bind:class="{ show: showAuthModal, 'd-block': showAuthModal }"
       tabindex="-1" role="dialog" ...>
  ```
- Add a backdrop div after the modal: `<div class="modal-backdrop fade show" v-if="showAuthModal" v-on:click="closeAuthModal()"></div>`

**6. Update `.compose/fragments/nav.html` — D-09 null guard and rename form:**

- Verify that `plannerStore?.listPlanners()` already uses optional chaining (line 34). If so, D-09 is already done.
- On the `<form id="rename">` element, add `v-show="renameVisible"` so it respects the new Vue flag.

**7. Delete bridge IIFE and jQuery CDN from `.compose/fragments/scripts.html` (per D-07):**

Remove these 3 CDN script tags entirely (lines 4-6):
```html
<script src="https://code.jquery.com/jquery-3.3.1.slim.min.js" ...></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js" ...></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js" ...></script>
```

Delete the entire `<script>(function() { ... })();</script>` IIFE block (lines 11-520). Keep ONLY:
```html
<script type="module" src="./js/main.js"></script>
```

The early dark-mode apply from URL (`?theme=dark`) and style-theme restore from localStorage that were in the bridge IIFE need to be preserved. Add a small inline script BEFORE main.js:
```html
<script>
// Early theme apply (before Vue mounts) — prevents flash
(function(){
    var saved = localStorage.getItem('style_theme') || 'ink';
    document.body.setAttribute('data-theme', saved);
    if (new URLSearchParams(window.location.search).get('theme') === 'dark') {
        document.body.classList.add('yp-dark');
    }
    if (sessionStorage.getItem('rail_open') === '1') {
        var r = document.getElementById('rail');
        if (r) r.classList.add('open');
        document.body.classList.add('rail-open');
        sessionStorage.removeItem('rail_open');
    }
})();
</script>
<script type="module" src="./js/main.js"></script>
```

**8. Run build:** `cd /Users/craig/src/github/alt-html/year-planner && .compose/build.sh`

**9. MOD-09 verification:** Confirm all 5 files in `.compose/fragments/modals/` (auth.html, delete.html, entry.html, feature.html, share.html) are referenced in `.compose/fragments/modals.html`. If `feature.html` is unreferenced (feature flags removed in Phase 11), remove its include from `modals.html` and optionally delete the file.
  </action>
  <verify>
    <automated>cd /Users/craig/src/github/alt-html/year-planner && .compose/build.sh && grep -c "jquery" .compose/fragments/scripts.html && grep -c "data-dismiss" .compose/fragments/modals/auth.html && grep "showAuthModal" .compose/fragments/modals/auth.html && grep "v-on:click" .compose/fragments/rail.html | head -5</automated>
  </verify>
  <acceptance_criteria>
    - .compose/fragments/scripts.html does NOT contain "jquery" (0 matches from grep -c)
    - .compose/fragments/scripts.html does NOT contain "popper" (0 matches)
    - .compose/fragments/scripts.html does NOT contain "bootstrap.min.js" (0 matches)
    - .compose/fragments/scripts.html does NOT contain "var rail = document.getElementById" (bridge deleted)
    - .compose/fragments/scripts.html contains `type="module" src="./js/main.js"`
    - .compose/fragments/scripts.html contains `data-theme` (early theme apply preserved)
    - .compose/fragments/rail.html contains `v-on:click="toggleFlyout"`
    - .compose/fragments/rail.html contains `v-on:click="toggleMarkerMode()"`
    - .compose/fragments/rail.html contains `v-on:click="toggleEmojiMode()"`
    - .compose/fragments/rail.html contains `v-on:click="showSignin()"`
    - .compose/fragments/rail.html contains `v-if="signedin"`
    - .compose/fragments/rail.html contains `v-show="railFlyout ===`
    - .compose/fragments/rail.html does NOT contain `id="railSigninBtn"` (bridge IDs removed)
    - .compose/fragments/modals/auth.html does NOT contain `data-dismiss="modal"` (0 matches)
    - .compose/fragments/modals/auth.html contains `v-on:click="closeAuthModal()"`
    - .compose/fragments/modals/auth.html contains `v-bind:class="{ show: showAuthModal`
    - site/js/vue/methods/auth.js contains `this.showAuthModal = true`
    - site/js/vue/methods/auth.js does NOT contain `$('#authModal')`
    - site/js/vue/methods/lifecycle.js contains `this.showAuthModal = true`
    - site/js/vue/methods/lifecycle.js does NOT contain `jQuery(`
    - site/js/vue/methods/planner.js contains `this.showShareModal = true`
    - site/js/vue/methods/planner.js contains `this.renameVisible = false`
    - site/js/vue/methods/planner.js does NOT contain `$('#rename').show()`
    - site/js/vue/methods/planner.js does NOT contain `$('#shareModal').modal(`
    - .compose/fragments/nav.html contains `v-show="renameVisible"` on the rename form
    - site/index.html exists (build.sh produced it)
    - site/index.html does NOT contain "jquery" (no jQuery in assembled output)
    - .compose/build.sh exits 0
  </acceptance_criteria>
  <done>All rail buttons are Vue-bound. All jQuery modal/show/hide calls replaced with Vue data flags. jQuery/Popper/Bootstrap JS CDN removed. Bridge IIFE deleted. site/index.html rebuilt. MOD-09 verified.</done>
</task>

<task type="auto">
  <name>Task 3: Update E2E test helpers for jQuery-free modal and verify existing tests pass</name>
  <read_first>
    - .tests/e2e/auth-modal.spec.js
    - .tests/globalSetup.js
    - .tests/playwright.config.js
  </read_first>
  <files>
    .tests/e2e/auth-modal.spec.js
  </files>
  <action>
**1. Update `openAuthModal()` helper in `.tests/e2e/auth-modal.spec.js`:**

The current helper calls `page.evaluate(() => jQuery('#authModal').modal('show'))` which will throw `jQuery is not defined` after jQuery removal.

Replace with a Vue-based trigger. The rail sign-in button is now inside `#app` with Vue bindings. Use:
```javascript
async function openAuthModal(page) {
    // Click the rail sign-in button (now a Vue-bound element inside #app)
    // First open the rail if not already open
    const rail = page.locator('#rail');
    if (!await rail.evaluate(el => el.classList.contains('open'))) {
        await page.locator('.rail-toggle').click();
    }
    await page.locator('#rail button[title="Sign in"]').click();
    await page.waitForSelector('#authModal.show');
}
```

Alternatively, if the rail button is hard to reach in test viewport, use `page.evaluate`:
```javascript
async function openAuthModal(page) {
    await page.evaluate(() => {
        const app = document.getElementById('app');
        if (app && app.__vue_app__) {
            const vm = app.__vue_app__._instance.proxy;
            vm.showSignin();
        }
    });
    await page.waitForSelector('#authModal.show');
}
```

Choose whichever approach works — the key is NO jQuery reference.

**2. Update Apple/Microsoft dismiss assertions (E2E-AUTH-02, E2E-AUTH-03):**

These tests currently rely on `data-dismiss="modal"` closing the modal. After the migration, Apple/Microsoft buttons call `signInWith('apple')` / `signInWith('microsoft')` which will attempt auth and may fail (providers not configured). The test expects the modal to close.

Since Apple/Microsoft providers are not configured (D-08), `signInWith()` will throw and the catch block sets `modalError`. The modal will NOT close automatically on error. Two options:

Option A: The test verifies the button click triggers the expected behavior (modal stays open with error for unconfigured providers). Update assertions to expect modal stays visible with an error message.

Option B: Add `this.showAuthModal = false` before the try block in `signInWith()` so modal closes immediately on click (matching old data-dismiss behavior). Then the test assertion stays the same.

Choose Option B to match existing test expectations and old UX behavior: in auth.js `signInWith()`, add `this.showAuthModal = false;` as the first line (before `this.clearModalAlert()`). This way the modal closes on button click regardless of auth outcome, matching the old `data-dismiss="modal"` behavior.

**3. Run existing E2E tests to verify no regressions:**

```bash
cd /Users/craig/src/github/alt-html/year-planner/.tests && npx playwright test --reporter=list
```

If tests fail, diagnose and fix. Common failure causes:
- `jQuery is not defined` in any test — search for remaining jQuery references
- Modal not showing — check showAuthModal data flag is reactive
- Rail buttons not clickable — check rail is inside #app in built index.html
  </action>
  <verify>
    <automated>cd /Users/craig/src/github/alt-html/year-planner/.tests && npx playwright test --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - .tests/e2e/auth-modal.spec.js does NOT contain `jQuery` (0 grep matches)
    - .tests/e2e/auth-modal.spec.js does NOT contain `$.modal` (0 grep matches)
    - All Playwright E2E tests pass (exit code 0)
    - auth-modal.spec.js E2E-AUTH-01 (Google sign-in button) passes
    - auth-modal.spec.js E2E-AUTH-02 (Apple dismiss) passes
    - auth-modal.spec.js E2E-AUTH-03 (Microsoft dismiss) passes
  </acceptance_criteria>
  <done>E2E test helpers updated for jQuery-free modal. All existing Playwright tests pass with no jQuery dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rail buttons -> Vue methods | User clicks on rail trigger Vue method calls; all are UI toggles with no auth/data sensitivity |
| modal show/hide | CSS class toggle replaces jQuery plugin; no security surface change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | Vue `_vnode.component.proxy` removal | mitigate | Bridge used private Vue internals to invoke methods from outside #app. Migration eliminates this attack surface — rail is now inside #app with direct Vue bindings. No external code can call Vue methods via `_vnode`. |
| T-12-02 | Information Disclosure | jQuery CDN removal | accept | jQuery 3.3.1 slim has known CVEs. Removing it eliminates a third-party script execution vector. No action needed — removal is the mitigation. |
| T-12-03 | Denial of Service | Early theme script inline | accept | Small inline script for theme flash prevention. No user input processed. No network requests. Low risk. |
</threat_model>

<verification>
1. `site/index.html` contains NO jQuery/Popper/Bootstrap JS script tags
2. `site/index.html` contains rail.html content INSIDE the `<div id="app">` element
3. All rail buttons have `v-on:click` attributes in the built index.html
4. Auth modal opens/closes via Vue data flag (no jQuery)
5. Marker and emoji modes work (flyouts open, cell painting functional)
6. All E2E tests pass
7. MOD-09: All modal fragments in `.compose/fragments/modals/` are accounted for
</verification>

<success_criteria>
- Zero jQuery/Popper/Bootstrap JS references in built site/index.html
- Rail fully Vue-controlled with v-on/v-if/v-show bindings
- Bridge IIFE deleted (scripts.html under 20 lines)
- All existing E2E tests pass
- showAuthModal/showShareModal/renameVisible Vue flags control modal visibility
</success_criteria>

<output>
After completion, create `.planning/phases/12-auth-client-configuration-live-sync/12-01-SUMMARY.md`
</output>
