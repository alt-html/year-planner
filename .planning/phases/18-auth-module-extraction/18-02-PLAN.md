---
phase: 18-auth-module-extraction
plan: "02"
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - site/js/config/contexts.js
  - site/js/service/AuthProvider.js
  - site/js/config/auth-config.js
autonomous: false
requirements: [AUT-04]
must_haves:
  truths:
    - "App boots and auth flow works end-to-end via the new auth module"
    - "Old AuthProvider.js does not exist"
    - "Old config/auth-config.js does not exist"
    - "contexts.js imports AuthService from ../auth/AuthService.js"
    - "Api.js and Application.js receive authProvider via CDI without code changes"
  artifacts:
    - path: "site/js/config/contexts.js"
      provides: "Updated CDI registration importing AuthService"
      contains: "import AuthService from '../auth/AuthService.js'"
    - path: "site/js/service/AuthProvider.js"
      provides: "DELETED — must not exist"
    - path: "site/js/config/auth-config.js"
      provides: "DELETED — must not exist"
  key_links:
    - from: "site/js/config/contexts.js"
      to: "site/js/auth/AuthService.js"
      via: "import and CDI Singleton registration"
      pattern: "new Singleton\\(AuthService\\)"
    - from: "site/js/auth/AuthService.js"
      to: "site/js/service/Api.js"
      via: "CDI injects authProvider param using qualifier match"
      pattern: "qualifier.*AuthProvider"
---

<objective>
Atomic CDI swap — delete old AuthProvider.js and config/auth-config.js, rewire contexts.js to import AuthService from the new auth module (AUT-04).

Purpose: Complete the extraction by switching the CDI wiring to the new module and removing old files. This is the final step that makes the auth module live.

Output: contexts.js imports from site/js/auth/, old files deleted, full E2E suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-auth-module-extraction/18-RESEARCH.md
@.planning/phases/18-auth-module-extraction/18-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output — the new auth module -->

From site/js/auth/AuthService.js:
```javascript
import { authConfig } from './auth-config.js';
import { OAuthClient } from './OAuthClient.js';
import { ClientAuthSession } from '../vendor/jsmdma-auth-client.esm.js';

export default class AuthService {
    constructor(model) {
        this.qualifier = '@alt-html/year-planner/AuthProvider' // CDI compat
        // ...
    }
    getAvailableProviders() { /* returns string[] */ }
    isConfigured() { /* returns boolean */ }
    async signIn(provider) { /* delegates to OAuthClient */ }
    signOut() { /* clears auth keys, sets model.signedin = false */ }
    getToken() { /* returns JWT string or null */ }
    getProvider() { /* returns provider name string or null */ }
}
```

From site/js/config/contexts.js (current — to be modified):
```javascript
import AuthProvider from '../service/AuthProvider.js';
// ...
new Singleton(AuthProvider),
```

CDI wiring note: AuthService constructor takes `model` only (not `model, storageLocal` like old AuthProvider). CDI resolves `model` from the registered `{ name: 'model', Reference: model }` entry. The `storageLocal` param is no longer needed since wipe() was removed in Plan 01.

Consumer note: Api.js constructor has param `authProvider` — CDI matches this against the qualifier `@alt-html/year-planner/AuthProvider` set inside AuthService. Application.js constructor has param `authProvider` — same CDI resolution. Neither file needs modification.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CDI swap — rewire contexts.js, delete old files (AUT-04)</name>
  <files>site/js/config/contexts.js, site/js/service/AuthProvider.js, site/js/config/auth-config.js</files>
  <read_first>
    - site/js/config/contexts.js (current CDI registration — see import path and Singleton line)
    - site/js/auth/AuthService.js (new module — verify it exists and has correct qualifier)
    - site/js/auth/auth-config.js (new config — verify it exists)
    - site/js/service/AuthProvider.js (old file — confirm it still exists before deletion)
    - site/js/config/auth-config.js (old config — confirm it still exists before deletion)
    - site/js/service/Api.js (lines 22-24 — confirm constructor param is `authProvider`)
    - site/js/Application.js (line 9 — confirm constructor param is `authProvider`)
  </read_first>
  <action>
1. Edit `site/js/config/contexts.js`:
   - Change the import line from:
     `import AuthProvider from '../service/AuthProvider.js';`
     to:
     `import AuthService from '../auth/AuthService.js';`
   - Change the Singleton registration from:
     `new Singleton(AuthProvider),`
     to:
     `new Singleton(AuthService),`
   - Remove the `import { authConfig } from ...` line if it exists (it should not — auth-config is imported inside AuthProvider/AuthService, not in contexts.js)
   - Leave all other imports and registrations unchanged

2. Delete `site/js/service/AuthProvider.js` — the old monolithic auth provider. All its functionality is now in site/js/auth/AuthService.js + OAuthClient.js.

3. Delete `site/js/config/auth-config.js` — the old config location. The config now lives at site/js/auth/auth-config.js.

4. Verify no other files import from the deleted paths:
   - Run: `grep -r "service/AuthProvider" site/js/` — must return nothing
   - Run: `grep -r "config/auth-config" site/js/` — must return nothing
   If any other file imports from these old paths, update those imports too. Based on research, no other files import these directly — AuthProvider is only imported in contexts.js, and auth-config.js is only imported in AuthProvider.js.

Do NOT modify Api.js, Application.js, vue/methods/auth.js, or vue/model.js — CDI wiring handles the injection automatically via the `authProvider` param name matching the `@alt-html/year-planner/AuthProvider` qualifier.
  </action>
  <verify>
    <automated>cd /Users/craig/src/github/alt-html/year-planner && ! test -f site/js/service/AuthProvider.js && ! test -f site/js/config/auth-config.js && grep -q "import AuthService from '../auth/AuthService.js'" site/js/config/contexts.js && echo "CDI swap complete" && cd .tests && npx playwright test e2e/boot.spec.js e2e/auth-modal.spec.js --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - site/js/config/contexts.js contains `import AuthService from '../auth/AuthService.js'`
    - site/js/config/contexts.js contains `new Singleton(AuthService)`
    - site/js/config/contexts.js does NOT contain `AuthProvider`
    - site/js/service/AuthProvider.js does NOT exist
    - site/js/config/auth-config.js does NOT exist
    - `grep -r "service/AuthProvider" site/js/` returns no matches
    - `grep -r "config/auth-config" site/js/` returns no matches
  </acceptance_criteria>
  <done>contexts.js imports AuthService from site/js/auth/, old AuthProvider.js and config/auth-config.js deleted, no dangling imports</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify auth module extraction end-to-end</name>
  <files>n/a</files>
  <action>Human verifies the complete auth module extraction works in the live app.</action>
  <what-built>Complete auth module extraction — new site/js/auth/ module is live, old files deleted, sign-out preserves planner data</what-built>
  <how-to-verify>
    1. Run full E2E suite: `cd .tests && npx playwright test --reporter=line`
       - All tests must pass (boot, auth-modal, signout-wipe, smoke)
    2. Start local dev server and open the app in browser
    3. Verify the auth modal appears with Google and GitHub buttons (Apple/Microsoft hidden)
    4. If possible: sign in with GitHub, verify sign-in works
    5. Sign out — verify planner data is still visible (not wiped)
    6. Check browser console for errors — no CDI wiring failures
  </how-to-verify>
  <verify>
    <automated>cd /Users/craig/src/github/alt-html/year-planner/.tests && npx playwright test --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - Full Playwright test suite exits 0
    - Human confirms app boots without console errors
    - Human confirms auth modal shows Google and GitHub buttons
    - Human confirms sign-out does not wipe planner data
  </acceptance_criteria>
  <done>Human approved: app boots, auth flow works via new module, sign-out preserves data, full E2E green</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDI wiring | AuthService must resolve to same qualifier as old AuthProvider — mismatch breaks all auth consumers |
| Import graph | Deleting old files must not leave dangling imports causing 404s at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Denial of Service | CDI qualifier mismatch in AuthService.js | mitigate | AuthService.js sets `this.qualifier = '@alt-html/year-planner/AuthProvider'` — same string Api.js and Application.js expect; full E2E suite validates boot and auth flow work |
| T-18-05 | Denial of Service | Dangling import of deleted AuthProvider.js | mitigate | grep verification after deletion confirms no remaining imports; full E2E suite catches runtime 404s |
| T-18-06 | Denial of Service | Dangling import of deleted config/auth-config.js | mitigate | grep verification after deletion; new AuthService.js imports from ./auth-config.js (same folder) |
</threat_model>

<verification>
1. `cd .tests && npx playwright test --reporter=line` — full E2E suite green
2. `! test -f site/js/service/AuthProvider.js` — old file deleted
3. `! test -f site/js/config/auth-config.js` — old config deleted
4. `grep -r "service/AuthProvider" site/js/` — no results
5. `grep -r "config/auth-config" site/js/` — no results
6. Human verification of live app auth flow
</verification>

<success_criteria>
- Old AuthProvider.js deleted — does not exist at site/js/service/AuthProvider.js
- Old auth-config.js deleted — does not exist at site/js/config/auth-config.js
- contexts.js imports AuthService from site/js/auth/AuthService.js
- Full E2E suite passes (boot, auth-modal, signout-wipe, smoke)
- No dangling imports to deleted file paths
- Human confirms: app boots, auth modal works, sign-out preserves data
</success_criteria>

<output>
After completion, create `.planning/phases/18-auth-module-extraction/18-02-SUMMARY.md`
</output>
