---
phase: 02-core-e2e-tests
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/e2e.yml
autonomous: true
gap_closure: true
requirements:
  - E2E-01
  - E2E-02
  - E2E-03

must_haves:
  truths:
    - "Pushing to main or opening a PR triggers the Playwright E2E suite automatically"
    - "CI runs the full test suite without a live CDN (CDN fixtures serve from committed files)"
    - "A failing test causes the CI job to exit non-zero and blocks the PR"
  artifacts:
    - path: ".github/workflows/e2e.yml"
      provides: "GitHub Actions workflow that runs the Playwright suite on push and pull_request"
      contains: "npx playwright test"
  key_links:
    - from: ".github/workflows/e2e.yml"
      to: ".tests/playwright.config.js"
      via: "cd .tests && npx playwright test"
      pattern: "npx playwright test"
    - from: ".github/workflows/e2e.yml"
      to: ".tests/fixtures/cdn.js"
      via: "fixtures are committed to git and served by page.route() at test runtime"
      pattern: "fixtures/"
---

<objective>
Add a GitHub Actions CI workflow that runs the Playwright E2E suite on every push and pull request.

Purpose: The phase goal states tests must "run reliably in CI." CDN fixture infrastructure is already complete (cdn.js + 11 fixture files committed). This plan wires those fixtures into a CI pipeline so tests execute automatically without a live CDN.

Output: `.github/workflows/e2e.yml` — a GitHub Actions workflow file that starts the Docker-served app (or http-server), runs the full Playwright suite from `.tests/`, and fails the build on any test failure.
</objective>

<execution_context>
@/Users/craig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/craig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-core-e2e-tests/02-RESEARCH.md

<interfaces>
<!-- Existing test infrastructure the CI workflow must invoke -->

From .tests/playwright.config.js (key settings):
- webServer: { command: 'http-server .. -p 8080', port: 8080, reuseExistingServer: false }
- testDir: '.'
- globalSetup: './globalSetup.js'
- storageState: '.auth/storageState.json' (generated by globalSetup)

From .tests/package.json (scripts available):
- `npx playwright test` — runs full suite (smoke + e2e)
- `npx playwright install --with-deps chromium` — installs Chromium + OS deps

Run command from project root perspective:
  cd .tests && npx playwright test
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub Actions E2E workflow</name>
  <files>.github/workflows/e2e.yml</files>
  <action>
Create `.github/workflows/e2e.yml` with the following structure:

- Trigger on: `push` (all branches) and `pull_request` (all branches)
- Runner: `ubuntu-latest`
- Job name: `e2e`

Steps in order:
1. `actions/checkout@v4` — check out repo
2. `actions/setup-node@v4` with `node-version: '20'` — Node for Playwright
3. `npm ci` run from `.tests/` — install Playwright + dependencies (uses existing `.tests/package.json`)
4. `npx playwright install --with-deps chromium` run from `.tests/` — install Chromium browser + OS-level deps (fonts, libs) required on Ubuntu
5. `npx playwright test --reporter=line` run from `.tests/` — execute full suite

The `playwright.config.js` `webServer` block starts `http-server .. -p 8080` automatically; no separate server start step needed. `http-server` is listed in `.tests/package.json` devDependencies, so `npm ci` installs it.

Use `working-directory: .tests` on the npm/playwright steps rather than `cd .tests &&` prefix — cleaner YAML.

Do NOT use Docker in CI — the workflow uses `http-server` (already configured in `playwright.config.js` webServer). Do NOT add caching (keep it simple and correct first).

The workflow file header comment should note: "CDN assets are served from .tests/fixtures/ via page.route() — no live CDN access needed."
  </action>
  <verify>
    <automated>cd /Users/craig/src/github/alt-html/year-planner && cat .github/workflows/e2e.yml | grep -E "npx playwright (install|test)|npm ci|checkout|setup-node" | wc -l</automated>
  </verify>
  <done>
`.github/workflows/e2e.yml` exists and contains: `on: [push, pull_request]`, `actions/checkout@v4`, `actions/setup-node@v4`, `npm ci`, `npx playwright install --with-deps chromium`, and `npx playwright test --reporter=line` — all scoped to `.tests/` working directory.
  </done>
</task>

</tasks>

<verification>
Verify the workflow file is valid YAML and contains all required CI steps:

```bash
cd /Users/craig/src/github/alt-html/year-planner
cat .github/workflows/e2e.yml
# Confirm: on push+PR, npm ci in .tests/, playwright install chromium, playwright test
```

If `yq` or `python3 -c "import yaml"` is available, validate YAML syntax:
```bash
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/e2e.yml'))" && echo "YAML valid"
```
</verification>

<success_criteria>
- `.github/workflows/` directory exists
- `.github/workflows/e2e.yml` exists and is valid YAML
- Workflow triggers on push and pull_request
- Workflow installs Chromium via `npx playwright install --with-deps chromium`
- Workflow runs `npx playwright test` from `.tests/`
- No reference to Docker or live CDN in the workflow
- Comment confirms CDN fixtures are served from `.tests/fixtures/`
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-e2e-tests/02-04-SUMMARY.md`
</output>
