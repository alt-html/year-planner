---
phase: 02-core-e2e-tests
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .tests/e2e/planner-management.spec.js
autonomous: true
gap_closure: true
requirements:
  - E2E-03

must_haves:
  truths:
    - "After rename, the test explicitly asserts the new planner name appears in the navbar"
    - "The rename outcome is verified by observable UI state, not only by form hiding"
  artifacts:
    - path: ".tests/e2e/planner-management.spec.js"
      provides: "E2E-03 spec with navbar-brand assertion after rename"
      contains: "toContainText('My Test Planner')"
  key_links:
    - from: ".tests/e2e/planner-management.spec.js"
      to: ".navbar-brand"
      via: "page.locator('.navbar-brand').first().toContainText('My Test Planner')"
      pattern: "navbar-brand.*My Test Planner"
---

<objective>
Add the missing navbar assertion to planner-management.spec.js so the rename step is fully verified.

Purpose: The ROADMAP success criterion for E2E-03 requires the user to "rename it" and see the outcome. Currently the spec waits for `#rename` to hide (confirming `renamePlanner()` executed) but does not assert the new name appears in `.navbar-brand`. This one-line addition closes the gap.

Output: `.tests/e2e/planner-management.spec.js` — same file, one `expect` assertion inserted after the rename form hides.
</objective>

<execution_context>
@/Users/craig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/craig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-e2e-tests/02-VERIFICATION.md

<interfaces>
<!-- Current spec state — the insertion point is after line 41 -->

From .tests/e2e/planner-management.spec.js (lines 36-43):
```javascript
  // Trigger blur to invoke focusout -> renamePlanner() (saves name, sets rename=false)
  await page.evaluate(() => {
    document.querySelector('#title')?.blur();
  });
  // Wait for rename form to hide (jQuery $().hide() called by renamePlanner())
  await page.waitForSelector('#rename', { state: 'hidden' });

  // --- Switch back to the original planner (triggers full-page navigation) ---
```

Insert after line 41 (`waitForSelector('#rename', { state: 'hidden' })`):
```javascript
  // Assert new planner name is visible in navbar
  await expect(page.locator('.navbar-brand').first()).toContainText('My Test Planner');
```

The `.navbar-brand` selector targets the first anchor in the navbar which displays the current planner name. `.first()` disambiguates from `.navbar-brand-support` (the secondary brand link). No navigation occurs between rename and this assertion — renamePlanner() updates reactive Vue state only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add navbar-brand assertion after rename</name>
  <files>.tests/e2e/planner-management.spec.js</files>
  <action>
Read the current `.tests/e2e/planner-management.spec.js`. After the line:

```javascript
  await page.waitForSelector('#rename', { state: 'hidden' });
```

Insert one new line (keeping the blank line separator before the next section comment):

```javascript
  // Assert new planner name is visible in navbar brand
  await expect(page.locator('.navbar-brand').first()).toContainText('My Test Planner');
```

The comment block `// --- Switch back to the original planner ---` that follows must remain on its own line after a blank line — preserve the existing whitespace style.

Do NOT change any other part of the spec. Do NOT reformat, re-order, or add any other assertions. The change is surgical: one assertion inserted in one location.
  </action>
  <verify>
    <automated>grep -n "navbar-brand.*My Test Planner\|toContainText.*My Test Planner" /Users/craig/src/github/alt-html/year-planner/.tests/e2e/planner-management.spec.js</automated>
  </verify>
  <done>
`.tests/e2e/planner-management.spec.js` contains `await expect(page.locator('.navbar-brand').first()).toContainText('My Test Planner')` immediately after the `waitForSelector('#rename', { state: 'hidden' })` line. All other lines in the file are unchanged.
  </done>
</task>

</tasks>

<verification>
Confirm the assertion is present and the spec structure is otherwise unchanged:

```bash
grep -n "navbar-brand\|My Test Planner\|hidden\|Switch back" /Users/craig/src/github/alt-html/year-planner/.tests/e2e/planner-management.spec.js
```

Expected output shows:
- Line with `waitForSelector('#rename', { state: 'hidden' })`
- Next non-blank line: `expect(page.locator('.navbar-brand').first()).toContainText('My Test Planner')`
- Followed by the "Switch back" section comment

Line count should be 76-77 (was 74, adding 2 lines: assertion + blank line or just 1 line).
</verification>

<success_criteria>
- `planner-management.spec.js` contains `toContainText('My Test Planner')` referencing `.navbar-brand`
- Assertion is placed after `waitForSelector('#rename', { state: 'hidden' })` and before the "Switch back" navigation section
- No other changes to the spec
- `grep -c "toContainText" .tests/e2e/planner-management.spec.js` returns at least 1
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-e2e-tests/02-05-SUMMARY.md`
</output>
